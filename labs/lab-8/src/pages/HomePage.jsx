import UrlShortener from "../components/UrlShortener";

/**
 * HomePage — renders the URL shortener form.
 * This is the default route ("/").
 */
function HomePage() {
  return (
    <main>
      <UrlShortener />
    </main>
  );
}

export default HomePage;
